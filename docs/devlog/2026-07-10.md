---
id: smart-health-cabin-devlog-2026-07-10
title: "2026-07-10 Devlog"
date: 2026-07-10
topic: smart-health-cabin
type: devlog
status: accepted
---

# 2026-07-10 Devlog

## Sprint Context

- Sprint: Sprint 5 live acceptance hardening.
- Planned finish line: real local ASR, Gemma 4 E4B, BreezyVoice default voice,
  provider acceptance status, live-check scripts, and five-run demo evidence.

## Actual Outcome

- Completed: provider status now exposes live acceptance eligibility for ASR,
  LLM, TTS, and Redpanda.
- Completed: ASR, LLM, and TTS provider status now exposes GPU-only acceptance
  controls: `computeBackend=gpu`, `gpuRequired=true`, `cpuOffload=false`, and
  `cpuFallbackAllowed=false`.
- Completed: LLM adapter accepts `VLLM_BASE_URL` / `VLLM_MODEL` and the current
  local OpenAI-compatible Gemma 4 E4B endpoint.
- Completed: ASR adapter accepts `ASR_LANGUAGE`; existing path config supports
  the current `http://localhost:8001/asr` service.
- Completed: TTS adapter and contract keep BreezyVoice on `default` voice and
  reject customized voice configuration or request fields.
- Completed: `live:check`, `smoke:api`, and `smoke:redpanda` scripts were added.
- Completed: `@shc/voice-agent-server` was added as a runnable process that
  proxies agent-turn routes and checks strict vLLM readiness.
- Completed: `smoke:sprint5-live-demo` was added as a repeatable five-run live
  demo script covering admin publish, active questionnaire load, vLLM guidance,
  BreezyVoice question audio, ASR answer transcription, deterministic answer
  mapping, questionnaire submit, safety route, and public report retrieval.
- Completed: strict local vLLM Gemma 4 E4B runs at `http://localhost:8000/v1`
  without CPU offload. `VLLM_USE_FLASHINFER_SAMPLER=0` is the current GPU-only
  workaround for missing system CUDA toolkit / FlashInfer sampler JIT.

## Validation

- `corepack pnpm test` passed with 51 tests.
- `corepack pnpm lint` passed across the workspace.
- `corepack pnpm typecheck` passed across the workspace.
- `corepack pnpm validate:json` passed.
- `corepack pnpm build` passed with existing Vite chunk-size warnings.
- `corepack pnpm smoke:redpanda` passed with Redpanda ready.
- `corepack pnpm smoke:api` passed and reported all required providers live,
  with strict Sprint 5 eligibility true.
- `corepack pnpm smoke:voice-agent` passed for the standalone
  voice-agent-server process; it reported API ready, vLLM ready, GPU-only LLM
  controls present, and strict `acceptanceEligible=true`.
- `corepack pnpm live:check` passed with ASR, vLLM, BreezyVoice, Redpanda, and
  voice-agent-server all strict eligible.
- `corepack pnpm smoke:sprint5-live-demo` passed five live runs using vLLM,
  BreezyVoice, ASR, deterministic answer mapping, report retrieval, and public
  report scope checks.
- Local API-level provider smoke confirmed ASR at port 8001, vLLM Gemma 4 E4B
  at port 8000, and BreezyVoice default voice at port 9003.
- Customized TTS voice request was rejected with HTTP 400.
- Outbox worker published 56 pending events to live Redpanda with
  `{"published":56,"failed":0}`; database status after worker was
  `published=82` and no pending rows.

## Scope Control

- Strict Sprint 5 acceptance uses GPU-only AI model inference. CPU backend,
  CPU fallback, or CPU offload is not acceptance eligible.
- The current vLLM output quality is controlled by the deterministic guidance
  fallback guard: the API only uses generated guidance when it contains usable
  Traditional Chinese; answer mapping remains deterministic and confirmation
  gated.
- BreezyVoice remains default-voice only. Customized voice fields and configured
  custom voice IDs remain rejected.

## Next Handoff

- Keep the strict GPU-only startup commands in
  `docs/ops/LIVE_PROVIDER_RUNBOOK.md` as the operator source of truth.
- For the next release layer, improve the vLLM Gemma tokenizer / chat template
  quality so deterministic guidance fallback is a safety net rather than the
  dominant user-facing path.
- Browser-level manual demo remains the stakeholder-facing presentation layer;
  the current Sprint 5 acceptance evidence proves the live API, voice-agent,
  report, and Redpanda spine.
