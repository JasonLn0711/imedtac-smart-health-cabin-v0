---
id: smart-health-cabin-devlog-2026-06-25
title: "2026-06-25 Smart Health Cabin Questionnaire + Avatar MVP Pivot"
date: 2026-06-25
topic: smart-health-cabin
type: devlog
status: active
source:
  - ../specs/MVP-QUESTIONNAIRE-AVATAR-SPRINT-PLAN.md
  - ../specs/MVP-SYSTEM-SPEC.md
  - ../../workstreams/smart-health-cabin/post-meeting-decision-log.md
  - ../../workstreams/smart-health-cabin/2026-06-25-questionnaire-avatar-mvp-pivot.md
  - ../../source/2026-06-25-duobao-line-questionnaire-avatar-mvp/source.md
---

# 2026-06-25 Smart Health Cabin Questionnaire + Avatar MVP Pivot

## Sprint Context

- Sprint: Sprint 0.
- Day: D1, `2026-06-25`.
- Planned finish line: preserve the 多寶 / Jason LINE source, narrow Phase 1 to
  questionnaire + Avatar, and seed the first questionnaire path.
- Current record purpose: set the active MVP direction without claiming a
  runnable implementation that has not yet been validated.

## Planning Decision

The active MVP target is now:

```text
open questionnaire platform + ASR/LLM/TTS Avatar Agent
```

Vision and hearing stay in the module registry as Phase 2 planned modules.

## Actual Outcome

- Completed: user-provided LINE source preserved in
  `source/2026-06-25-duobao-line-questionnaire-avatar-mvp/source.md`.
- Completed: PHQ-9 PDF and agent-readable copy preserved under the source
  bundle and copied into `modules/questionnaire/source/`.
- Completed: PHQ-9 SurveyJS seed and public scoring config added under
  `modules/questionnaire/`.
- Completed: active sprint plan added in
  `docs/specs/MVP-QUESTIONNAIRE-AVATAR-SPRINT-PLAN.md`.
- Completed: module manifests mark `questionnaire` and `avatar_agent` as Phase
  1 core, with `vision` and `hearing` as Phase 2 planned.
- Validation: documentation and JSON validation only for this sync;
  implementation validation remains attached to the next runnable closeout.

## Scope Control

- Phase 1 owns configurable questionnaire flow, PHQ-9 seed, public report route,
  QR access, Avatar voice guidance, persistence, and outbox events.
- Vision/hearing are planned Phase 2 modules.
- Public PHQ-9 output stays self-check / staff-review support and does not show
  diagnostic claims.

## Next Handoff

- Next action: Sprint 0 D2 implementation closeout should produce the smallest
  runnable skeleton for questionnaire/API/storage, or record the exact blocker.
- Next planning action: `../planning-everything-track` should record only
  locator, status, capacity, blocker, validation evidence, and next gate.

## Sprint 5.6 Voice Entry Activation Gate Closeout

Record date: `2026-06-25`.

Capability:

- Implemented a local wake word activation gate with tap-to-start fallback.
- Added `apps/model-sidecars/wakeword-service/` as a FastAPI sidecar on port
  `8013`.
- Added a provider-swappable wakeword sidecar path; the active provider is now
  recorded in the sherpa-onnx replacement section below.
- Updated the kiosk Avatar XState voice-entry states to:
  `idle_touch_ready`, `wake_armed`, `wake_detected`, `recording_answer`,
  `endpointing_wait`, `transcribing`, `confirming_candidate`, `committed`,
  `retry_or_touch`, `voice_unavailable`, and `staff_review`.
- Kept wake word as activation only. Questionnaire writes remain gated by ASR
  candidate mapping and user confirmation.

Evidence:

- `apps/model-sidecars/wakeword-service/app.py` exposes `/healthz`, `/status`,
  `/simulate-wake`, and `WS /events`.
- `apps/kiosk-web/src/features/avatar/avatarStateMachine.test.ts` covers wake
  activation, tap-to-start fallback, low-confidence routing, and confirmation
  before commit.
- `apps/kiosk-web/src/features/avatar/AvatarPanel.tsx` keeps the
  `/simulate-wake` UI dev-only through `import.meta.env.DEV`.
- `.env.example` records the voice-entry and endpointing settings.

Validation:

- `corepack pnpm test` passed: 55 tests.
- `corepack pnpm typecheck` passed.
- `corepack pnpm build` passed, with existing Vite large-chunk warnings.
- `corepack pnpm validate:json` passed.
- `git diff --check` passed.
- Wakeword sidecar unittest passed in a temporary test venv.
- Playwright rendered smoke verified:
  wake simulation enters the voice path, no-speech routes to retry/touch,
  candidate mapping requires confirmation, confirmed candidate writes, and
  tap-to-start enters recording on mobile viewport.

Scope Control:

- No formal Mandarin wake phrase was trained in this closeout.
- The current endpointing gate uses a small browser RMS threshold as a
  testable interim gate; replace it with Silero VAD ONNX once kiosk hardware
  and runtime constraints are fixed.
- Raw audio retention remains disabled by default.

Next Handoff:

- Select and test the formal Mandarin wake phrase.
- Run onsite/noisy-environment false-trigger and miss-rate testing.
- Replace the interim browser RMS endpointing gate with Silero VAD or a local
  sidecar VAD path after hardware selection.

## Continuous Wakeword-to-VAD-to-TTS Loop Update

Record date: `2026-06-25`.

Capability:

- Connected `wake.detected` to continuous kiosk voice mode.
- Each voice turn now records through browser endpointing, sends ASR, requests a
  guidance response, synthesizes TTS, plays the returned audio, and automatically
  returns to the next recording turn.
- Clear option matches now write the current answer, advance the UI to the next
  question, and ask that same next question through TTS.
- Added a stop control that cancels the current capture and returns the Avatar
  to touch-ready idle.
- Kept questionnaire writes gated by candidate mapping; unmapped speech does not
  write or advance.

Evidence:

- `apps/kiosk-web/src/features/avatar/voiceAgentApi.ts` owns the kiosk calls to
  `/api/v1/agent-sessions`, `/api/v1/agent-turns/asr`,
  `/api/v1/agent-turns/respond`, and `/api/v1/agent-turns/tts`.
- `apps/kiosk-web/src/features/avatar/avatarStateMachine.ts` adds the
  `LOOP_READY` transition from `transcribing` back to `recording_answer`.
- `apps/kiosk-web/src/features/avatar/AvatarPanel.tsx` tracks continuous mode,
  completed voice turns, stop cancellation, and a hard max recording timeout.
- `apps/kiosk-web/src/features/avatar/voiceQuestionnaireController.ts` exposes a
  shared answer-write-and-next-question helper for manual and continuous paths.

Validation:

- `corepack pnpm test apps/kiosk-web/src/features/avatar/avatarStateMachine.test.ts apps/kiosk-web/src/features/avatar/voiceQuestionnaireController.test.ts`
  passed: 22 tests.
- `corepack pnpm --filter @shc/kiosk-web typecheck` passed.
- API smoke confirmed `/api/v1/agent-turns/asr`, `/respond`, and `/tts` return
  mock provider responses and playable TTS data URL.
- Playwright on a temporary short-timeout kiosk verified:
  wake simulation -> recording -> answer summary + TTS reply -> next recording
  turn -> stop returns to idle.
- Detailed live experiment data is preserved in
  `docs/evidence/2026-06-25-wakeword-continuous-live-voice-loop-experiment-log.md`,
  including hardware/GPU inventory, provider commands, port/PID map, live
  acceptance output, TTS-to-ASR probes, and one-turn plus two-turn browser
  validation.
- Live browser validation advanced `phq9_01 -> phq9_02 -> phq9_03`; each TTS
  turn contained the answer summary, supportive feedback, and next-question
  prompt while the UI advanced to the same next question.

Scope Control:

- The browser RMS endpointing gate remains the local MVP gate until Silero VAD
  or a sidecar VAD path is selected against kiosk hardware.
- Live wakeword activation remains local-only through sherpa-onnx.
- The voice loop provides guidance and response playback; it does not diagnose,
  alter scoring, or write unmapped speech.

## Sprint 5.7 Kiosk One-Question Avatar Rail Closeout

Record date: `2026-06-25`.

Capability:

- Updated kiosk questionnaire rendering so PHQ-9 appears one question at a
  time through SurveyJS native `questionsOnPageMode: "questionPerPage"`.
- Preserved explicit final submission by using SurveyJS
  `goNextPageAutomatic=true` with `allowCompleteSurveyAutomatic=false`.
- Redesigned the kiosk interaction surface into a desktop/kiosk two-column
  layout: replaceable Avatar rail on the left third, active SurveyJS question
  on the right two-thirds.
- Kept the narrow viewport layout stacked so the Avatar rail remains above the
  question on mobile-sized screens.
- Kept `VITE_AVATAR_IMAGE_SRC` as the visual asset swap point and updated
  outward UI language to `AI 健康互動助理`.
- Updated the voice-question controller so voice confirmation maps to the
  currently visible SurveyJS question, including previous/next navigation,
  rather than the first unanswered question.

Evidence:

- `apps/kiosk-web/src/features/questionnaire/SurveyJsQuestionnaireRenderer.tsx`
  owns the kiosk SurveyJS model defaults and the Avatar/question layout shell.
- `apps/kiosk-web/src/features/avatar/AvatarPanel.tsx` listens to SurveyJS page
  and value changes, then advances the SurveyJS question after confirmed voice
  writes.
- `apps/kiosk-web/src/features/avatar/voiceQuestionnaireController.ts` exposes
  `getCurrentSurveyQuestion()` for SurveyJS `currentSingleQuestion` first, with
  fallback to current page and unanswered question lookup.
- `apps/kiosk-web/src/styles/app.css` owns the 1:2 desktop rail/question layout
  and the stacked mobile layout.

Validation:

- `corepack pnpm typecheck` passed.
- `corepack pnpm test` passed: 57 tests.
- `corepack pnpm build` passed, with existing Vite large-chunk warnings.
- Playwright rendered verification passed for:
  desktop avatar rail / question two-column ratio, touch answer advancing from
  question 1 to question 2, explicit final submit, mobile stacked layout, and
  replaceable avatar image rendering.

Scope Control:

- Backend scoring, response persistence, report generation, ASR, LLM, TTS, and
  Redpanda behavior were not changed.
- The current visual asset remains a static replaceable Avatar image. It is not
  a real physician identity and does not imply diagnosis or treatment.

Next Handoff:

- Replace the static Avatar asset with a professional synthetic human doctor
  image when the stakeholder approves visual style.
- Keep the left Avatar rail component boundary stable so a future live Avatar
  runtime can replace the image without changing SurveyJS questionnaire logic.

## Sprint 5.6 Superseded Wakeword Route

Record date: `2026-06-25`.

Superseded status:

- Extended `wakeword-service` beyond simulated events with a `WAKE_WORD_MODE`
  switch for `mock` and `live`.
- Added `smoke:wakeword` as a repo-level simulated wakeword smoke path.
- The earlier external-key/provider-specific route is no longer active. The
  current runtime is the sherpa-onnx KWS route recorded below.

## Sprint 5.6 Wakeword Provider Replacement: sherpa-onnx KWS

Record date: `2026-06-25`.

Capability:

- Replaced the blocked Porcupine route with `sherpa-onnx KWS Zipformer zh-en
  3M` as the active local wakeword provider.
- Fixed the formal wake phrase as `你好小慧`.
- Removed Picovoice AccessKey and Porcupine `.ppn/.pv` requirements from the
  runtime path.
- Added sherpa-onnx model-file readiness checks for encoder, decoder, joiner,
  tokens, and the generated keyword file.
- Generated the local keyword token file from `你好小慧 @你好小慧`; expected token
  line is `n ǐ h ǎo x iǎo h uì @你好小慧`.

Evidence:

- `.env.example` now records `WAKE_WORD_PROVIDER=sherpa-onnx`,
  `WAKE_WORD_PHRASE=你好小慧`, `SHERPA_ONNX_KWS_MODEL_DIR`,
  `SHERPA_ONNX_KWS_*` model paths, and
  `SHERPA_ONNX_KWS_KEYWORDS=.local/models/wakeword/ni-hao-xiao-hui.keywords.txt`.
- `apps/model-sidecars/wakeword-service/app.py` now uses
  `sherpa_onnx.KeywordSpotter` as the only live wakeword runtime.
- `apps/model-sidecars/wakeword-service/requirements.txt` removes
  `openwakeword` and `pvporcupine`, and adds `sherpa-onnx`, `sentencepiece`,
  and `pypinyin`.
- `apps/model-sidecars/wakeword-service/README.md`,
  `docs/dev/LOCAL_DEV.md`, and `docs/ops/LIVE_PROVIDER_RUNBOOK.md` document
  the install, keyword-generation, mock, live, and smoke-test flow.

Scope Control:

- Wakeword remains activation-only. It does not perform ASR, diagnosis, free
  conversation, questionnaire writes, production audio retention, or HIS
  behavior.
- Tap-to-start remains the fallback while microphone index, sensitivity,
  false-trigger rate, and miss-rate behavior are tuned.
- Porcupine and openWakeWord are no longer active runtime paths in this repo.

## Wake Greeting Live Test Evidence Update

Record date: `2026-06-25`.
Record time: `2026-06-25T23:46:50+08:00` to
`2026-06-25T23:50:46+08:00`.

Capability:

- Added the wake-greeting validation layer before the first questionnaire
  prompt.
- Confirmed the local test stack at `http://localhost:5176/` uses API `3010`,
  voice-agent `3011`, kiosk `5176`, live sherpa-onnx wakeword `8013`, live
  Breeze ASR `8001`, live Ollama Gemma 4 E4B `11434`, live BreezyVoice `9003`,
  and Redpanda `9092/9644`.
- Confirmed the browser request order:
  `purpose=wake_greeting` -> greeting TTS -> `question_name=phq9_01` ->
  first-question TTS.

Evidence:

- Full experiment detail is preserved in
  `docs/evidence/2026-06-25-wakeword-continuous-live-voice-loop-experiment-log.md`
  under `Experiment Run: Wake Greeting Then First Question TTS`.
- The evidence log now includes timestamp discipline, hardware/OS/GPU snapshot,
  runtime service PID/port map, provider readiness, live acceptance output,
  direct API smoke output, browser request-level smoke output, and focused test
  results.
- `docs/evidence/README.md` now records the required fields for every future
  experiment log entry, including local date/time and UTC timestamps.

Scope Control:

- The browser validation used simulated wake to make the request sequence
  deterministic; physical spoken wakeword and spoken-answer validation remain
  the next hardware-layer pass.
- The project remains in screening-support and human-review workflow scope.

## Answer Follow-Up Guidance Evidence Update

Record date: `2026-06-25`.
Record time: `2026-06-25T23:58:07+08:00`
(`2026-06-25T15:58:07+00:00`).

Capability:

- Added `purpose=answer_followup` on the existing agent respond route.
- The kiosk now sends the confirmed answer text, ASR transcript, current
  question, and next question to the API after confirmation.
- The API asks the live LLM for a short response summary and next-question
  prompt, with deterministic fallback text ready for service continuity.

Evidence:

- Full code-check evidence is preserved in
  `docs/evidence/2026-06-25-wakeword-continuous-live-voice-loop-experiment-log.md`
  under `Experiment Run: Answer Follow-Up Guidance Code Check`.
- Focused Vitest passed with `60` tests, and API/kiosk/voice-safety typechecks
  plus `git diff --check` passed.

Scope Control:

- The LLM phrases the response summary and next-question wording.
- Questionnaire writes remain confirmation-gated; the LLM does not write
  answers, alter scoring, create answer categories, or diagnose.
