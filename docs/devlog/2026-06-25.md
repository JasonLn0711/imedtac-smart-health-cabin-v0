---
id: smart-health-cabin-devlog-2026-06-25
title: "2026-06-25 Smart Health Cabin Questionnaire + Avatar MVP Pivot"
date: 2026-06-25
topic: smart-health-cabin
type: devlog
status: active
source:
  - ../specs/MVP-QUESTIONNAIRE-AVATAR-SPRINT-PLAN.md
  - ../specs/MVP-SYSTEM-SPEC.md
  - ../../workstreams/smart-health-cabin/post-meeting-decision-log.md
  - ../../workstreams/smart-health-cabin/2026-06-25-questionnaire-avatar-mvp-pivot.md
  - ../../source/2026-06-25-duobao-line-questionnaire-avatar-mvp/source.md
---

# 2026-06-25 Smart Health Cabin Questionnaire + Avatar MVP Pivot

## Sprint Context

- Sprint: Sprint 0.
- Day: D1, `2026-06-25`.
- Planned finish line: preserve the 多寶 / Jason LINE source, narrow Phase 1 to
  questionnaire + Avatar, and seed the first questionnaire path.
- Current record purpose: set the active MVP direction without claiming a
  runnable implementation that has not yet been validated.

## Planning Decision

The active MVP target is now:

```text
open questionnaire platform + ASR/LLM/TTS Avatar Agent
```

Vision and hearing stay in the module registry as Phase 2 planned modules.

## Actual Outcome

- Completed: user-provided LINE source preserved in
  `source/2026-06-25-duobao-line-questionnaire-avatar-mvp/source.md`.
- Completed: PHQ-9 PDF and agent-readable copy preserved under the source
  bundle and copied into `modules/questionnaire/source/`.
- Completed: PHQ-9 SurveyJS seed and public scoring config added under
  `modules/questionnaire/`.
- Completed: active sprint plan added in
  `docs/specs/MVP-QUESTIONNAIRE-AVATAR-SPRINT-PLAN.md`.
- Completed: module manifests mark `questionnaire` and `avatar_agent` as Phase
  1 core, with `vision` and `hearing` as Phase 2 planned.
- Validation: documentation and JSON validation only for this sync;
  implementation validation remains attached to the next runnable closeout.

## Scope Control

- Phase 1 owns configurable questionnaire flow, PHQ-9 seed, public report route,
  QR access, Avatar voice guidance, persistence, and outbox events.
- Vision/hearing are planned Phase 2 modules.
- Public PHQ-9 output stays self-check / staff-review support and does not show
  diagnostic claims.

## Next Handoff

- Next action: Sprint 0 D2 implementation closeout should produce the smallest
  runnable skeleton for questionnaire/API/storage, or record the exact blocker.
- Next planning action: `../planning-everything-track` should record only
  locator, status, capacity, blocker, validation evidence, and next gate.

## Sprint 5.6 Voice Entry Activation Gate Closeout

Record date: `2026-06-25`.

Capability:

- Implemented a local wake word activation gate with tap-to-start fallback.
- Added `apps/model-sidecars/wakeword-service/` as a FastAPI sidecar on port
  `8013`.
- Selected `openWakeWord` as the first local provider and kept Picovoice
  Porcupine as the commercial fallback path in the SDD.
- Updated the kiosk Avatar XState voice-entry states to:
  `idle_touch_ready`, `wake_armed`, `wake_detected`, `recording_answer`,
  `endpointing_wait`, `transcribing`, `confirming_candidate`, `committed`,
  `retry_or_touch`, `voice_unavailable`, and `staff_review`.
- Kept wake word as activation only. Questionnaire writes remain gated by ASR
  candidate mapping and user confirmation.

Evidence:

- `apps/model-sidecars/wakeword-service/app.py` exposes `/healthz`, `/status`,
  `/simulate-wake`, and `WS /events`.
- `apps/kiosk-web/src/features/avatar/avatarStateMachine.test.ts` covers wake
  activation, tap-to-start fallback, low-confidence routing, and confirmation
  before commit.
- `apps/kiosk-web/src/features/avatar/AvatarPanel.tsx` keeps the
  `/simulate-wake` UI dev-only through `import.meta.env.DEV`.
- `.env.example` records the voice-entry and endpointing settings.

Validation:

- `corepack pnpm test` passed: 55 tests.
- `corepack pnpm typecheck` passed.
- `corepack pnpm build` passed, with existing Vite large-chunk warnings.
- `corepack pnpm validate:json` passed.
- `git diff --check` passed.
- Wakeword sidecar unittest passed in a temporary test venv.
- Playwright rendered smoke verified:
  wake simulation enters the voice path, no-speech routes to retry/touch,
  candidate mapping requires confirmation, confirmed candidate writes, and
  tap-to-start enters recording on mobile viewport.

Scope Control:

- No formal Mandarin wake phrase was trained in this closeout.
- The current endpointing gate uses a small browser RMS threshold as a
  testable interim gate; replace it with Silero VAD ONNX once kiosk hardware
  and runtime constraints are fixed.
- Raw audio retention remains disabled by default.

Next Handoff:

- Select and test the formal Mandarin wake phrase.
- Run onsite/noisy-environment false-trigger and miss-rate testing.
- Replace the interim browser RMS endpointing gate with Silero VAD or a local
  sidecar VAD path after hardware selection.
