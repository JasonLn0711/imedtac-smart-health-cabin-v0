# 2026-07-07 Sprint 4 D1 Closeout

## Entry Baseline

- Sprint 3 voice Agent turn reused: kiosk voice controls use the same
  deterministic mapping and confirmation rule.
- Sprint 2 report path preserved: completed questionnaire still returns a
  public report URL.

## Result

- Avatar UI: complete. Kiosk displays an Avatar Agent panel above the SurveyJS
  questionnaire.
- Avatar states: complete. Visible state labels include `待命`, `說明中`,
  `聆聽中`, `語音辨識中`, `整理回應中`, `等待確認`, and
  `可改用觸控填答`.
- Voice-confirmed PHQ-9 answers: complete. Browser smoke confirmed `3` answers
  by mock voice flow before touch-completing the rest.
- Public report and touch fallback: complete. Kiosk submit still uses the
  existing API and returns QR-ready public report URL.

## Evidence

- Playwright kiosk smoke:
  - loaded `9` SurveyJS questions;
  - Avatar panel was visible;
  - confirmed `3` answers through Read -> Map -> Confirm Answer;
  - touched remaining answers;
  - submitted through existing response API;
  - displayed QR URL
    `http://localhost:3000/api/v1/reports/rpt_4f416472-9fea-41f9-9a7b-09552b3a3af0/public`;
  - active questionnaire and completion output contained no forbidden public
    wording: `憂鬱症`, `中度憂鬱`, `重度憂鬱`, `診斷`, or `治療建議`.

## Blocker Or Next Action

- Sprint 4 D1 is closed. Sprint 4 D2 owns outbox-worker and Redpanda event
  visibility.
