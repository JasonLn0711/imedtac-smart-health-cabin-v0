# 2026-07-08 Sprint 4 D2 Closeout

## Entry Baseline

- Sprint 4 D1 Avatar state path reused: Avatar UI and voice-confirmed answer
  path passed browser smoke.
- Existing outbox rows / event seam reused: API writes outbox rows inside the
  same transaction as domain writes.

## Result

- Outbox rows: complete for questionnaire response, report created, agent turn,
  and audit event.
- Redpanda topics: complete. Topics visible through `rpk topic list`:
  `shc.questionnaire.responses.v1`, `shc.agent.turns.v1`,
  `shc.report.events.v1`, and `shc.audit.events.v1`.
- Redpanda Console: service started on local compose at port `8080`.
- E2E path: complete. PHQ-9 -> Avatar state UI -> voice-confirmed answers ->
  response submit -> public report URL -> outbox -> Redpanda publish.
- Redpanda failure behavior: API/kiosk does not depend on Redpanda env vars or
  synchronous publish. Worker failure leaves rows retryable; tests cover this.

## Evidence

- Docker / worker:
  - `docker compose -f infra/docker-compose.yml up -d redpanda redpanda-console`
    succeeded.
  - `corepack pnpm --filter @shc/outbox-worker start` published `15` pending
    events, then `6` newer pending events, then `5` final smoke events, all
    with `failed: 0`.
- Database rows after final worker run:
  - `8` questionnaire responses;
  - `8` report sections;
  - `5` public report tokens;
  - all outbox events were `published`.
- Redpanda topics from `rpk topic list`:
  - `shc.agent.turns.v1`;
  - `shc.audit.events.v1`;
  - `shc.questionnaire.responses.v1`;
  - `shc.report.events.v1`.

## Blocker Or Next Action

- Sprint 4 is closed. Redpanda event visibility is verified through `rpk`; no
  Redpanda environment blocker remains for the local demo path.
