# 2026-07-09 Sprint 4.5 Closeout

## Sprint Context

- Sprint: 4.5
- Planned finish line: freeze ASR / LLM / TTS / Avatar / runtime choices and
  make provider mode, fallback, and local sidecar contracts concrete.

## Actual Outcome

- ASR provider: selected and wired as `faster_whisper_breeze_asr_26` with
  Breeze-ASR-26 CT2 int8 sidecar contract.
- LLM provider: selected and wired as `vllm_openai_compatible` with
  OpenAI-compatible `/chat/completions` request shape for local Gemma 4 E4B.
- TTS provider: selected and wired as `breezyvoice_default`; custom voice,
  reference audio, speaker embedding, and voice-cloning fields are rejected.
- Provider status: `GET /api/v1/providers/status` reports `mock`, `live`, or
  `unavailable`.
- Static Avatar image path: `/avatar/default-avatar.svg`.
- Avatar state: `xstate` + `@xstate/react`; Sprint 4.5 recording/writing path
  is covered by unit test.
- Browser capture: `MediaRecorder` is the selected capture path; text mock
  remains the fallback.
- Contracts: provider status, ASR, LLM, TTS, Avatar state, and provider mode
  DTOs are in `packages/contracts` as Zod schemas.
- Sidecars: FastAPI ASR and TTS sidecar scaffolds exist under
  `apps/model-sidecars/`.

## Live / Fallback Status

- ASR live status: verified through compatibility service
  `http://localhost:8001` with `ASR_TRANSCRIBE_PATH=/asr`; returned transcript
  `完全沒有`. Canonical Sprint 4.5 sidecar path remains
  `http://localhost:8011/v1/asr/transcribe`.
- LLM live status: verified through OpenAI-compatible local endpoint
  `http://localhost:11434/v1` with model `gemma4:e4b`; provider status reports
  `live`.
- TTS live status: verified through BreezyVoice upstream compatibility
  `http://localhost:9003/v1`; API TTS returned a `data:audio/wav;base64,`
  payload. Canonical Sprint 4.5 sidecar path remains
  `http://localhost:8012/v1/tts/synthesize`.
- Mock fallback status: intact. Sprint 4 response/report/outbox path remains
  independent from live model providers.
- Mandarin / Taigi / mixed ASR test status: documented in
  `docs/handoff/sprint-4.5-model-selection.md`. Current automated coverage
  protects config/request shape and fallback behavior; measured Taigi/mixed
  audio fixtures remain a provider-validation layer.
- Sprint 4 regression status: covered by existing questionnaire, Avatar,
  voice-confirmation, report, and outbox tests.

## Evidence

- Commands:
  - `corepack pnpm --filter @shc/api-server test`
  - `corepack pnpm --filter @shc/kiosk-web typecheck`
  - `corepack pnpm --filter @shc/contracts typecheck`
  - `corepack pnpm test`
  - `corepack pnpm typecheck`
  - `corepack pnpm validate:json`
  - `python3 -m py_compile apps/model-sidecars/asr-service/app.py apps/model-sidecars/tts-service/app.py`
  - `git diff --check`
- Live API smoke:
  - mock provider status route returned all providers as `mock` and ready;
  - live provider status route returned ASR / LLM / TTS as `live` and ready;
  - live ASR returned provider `faster_whisper_breeze_asr_26`, model
    `Breeze-ASR-26-CT2-int8`, transcript `完全沒有`;
  - live LLM returned provider `vllm_openai_compatible`, model `gemma4:e4b`;
  - live TTS returned provider `breezyvoice_default`, model
    `MediaTek-Research/BreezyVoice`, and WAV data URL prefix;
  - custom TTS field `reference_audio_base64` returned HTTP `400`.
- Files changed:
  - `.env.example`
  - `packages/contracts/src/index.ts`
  - `apps/api-server/src/services/questionnaireService.ts`
  - `apps/api-server/src/routes/questionnaireRoutes.ts`
  - `apps/kiosk-web/src/features/avatar/*`
  - `apps/kiosk-web/public/avatar/default-avatar.svg`
  - `apps/model-sidecars/asr-service/*`
  - `apps/model-sidecars/tts-service/*`
  - `docs/handoff/sprint-4.5-model-selection.md`

## Scope Control

- Sprint 4.5 does not add animated Avatar, lip-sync, Live2D, 3D, reference
  audio, custom voice, voice cloning, production diagnosis, or treatment advice.
- Redpanda remains async. Live AI providers do not block kiosk completion,
  report creation, QR-ready public report URL, or touch fallback.

## Next Handoff

- Next action: Sprint 5 should replay the full demo path with the provider-mode
  matrix visible, then write deployment/API/DB known-limitation docs.
- Owner: Sprint 5 implementation agent.
