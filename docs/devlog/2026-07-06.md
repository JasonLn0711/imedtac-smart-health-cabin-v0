# 2026-07-06 Sprint 3 D2 Closeout

## Entry Baseline

- Sprint 3 D1 voice loop reused: ASR/respond/TTS/map-answer routes are live.
- Sprint 2 report path preserved: questionnaire response still creates public
  report token and safe public report.

## Result

- PHQ-9 question/options source: complete. Voice guidance reads from active
  SurveyJS JSON, not a second hard-coded questionnaire engine.
- Spoken answer mapping: complete. Deterministic mapping handles `完全沒有`,
  `幾天`, `一半以上`, and `幾乎每天`.
- Confirmation boundary: complete. Voice candidate requires user confirmation
  before writing into SurveyJS state.
- Touch fallback: complete. Kiosk keeps the SurveyJS touch form available while
  Avatar voice controls are visible.
- Safety boundary: complete. Backend scoring and public summary remain the only
  scoring/report authority.

## Evidence

- Unit tests:
  - Avatar state machine transitions;
  - voice answer confirmation writes only after confirm;
  - reject/retry path does not write an answer;
  - at least `3` PHQ-9 answers can be filled by mocked voice-confirmed path;
  - invalid transcript returns no candidate.
- Live map-answer route returned candidate value `3`, text `幾乎每天`,
  `requires_confirmation: true`.
- `corepack pnpm test` passed: `7` files, `37` tests.

## Blocker Or Next Action

- Sprint 3 is closed with deterministic provider mocks. Sprint 4 can attach a
  visible Avatar state UI and Redpanda outbox worker without changing the
  questionnaire engine.
