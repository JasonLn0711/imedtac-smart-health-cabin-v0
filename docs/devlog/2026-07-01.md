# 2026-07-01 Sprint 2 D1 Closeout

## Entry Baseline

- Sprint 0/1 regression check: `corepack pnpm validate:json`, `corepack pnpm
  test`, and `git diff --check` passed before implementation.
- PHQ-9 runtime reused: existing active questionnaire route, SurveyJS seed,
  scoring helper, and response submission path stayed in place.
- No PHQ-9 runtime rebuild: Sprint 2 added CMS/versioning around the existing
  path.

## Result

- Questionnaire template list: complete through
  `GET /api/v1/admin/questionnaire-templates`.
- JSON edit/validate: complete through admin textarea plus backend
  `validateSurveyJsQuestionnaire`.
- SurveyJS preview: complete in `apps/admin-web` with the same SurveyJS runtime
  family used by kiosk.
- Publish active version: complete through
  `POST /api/v1/admin/questionnaire-versions/{id}/publish`; previous active
  version archives and new version becomes active.
- Audit event: create and publish actions write audit rows and outbox events.

## Evidence

- Commands:
  - `corepack pnpm typecheck`
  - `corepack pnpm test`
  - `corepack pnpm validate:json`
  - `corepack pnpm lint`
  - `corepack pnpm build`
  - `git diff --check`
- Live API smoke:
  - created draft version `0.2.1782370632`;
  - published version id `1aa2519c-f9d4-49d4-a69f-b0416f527f8e`;
  - `GET /api/v1/questionnaires/active` returned active version
    `0.2.1782370632`.
- Admin browser smoke:
  - `http://localhost:5174/` loaded template list;
  - Validate / Preview rendered `9` SurveyJS questions.

## Blocker Or Next Action

- Sprint 2 D1 is closed. Sprint 2 D2 owns report section, public token / QR URL,
  admin response list, and public report filtering.

