# 2026-07-02 Sprint 2 D2 Closeout

## Entry Baseline

- Sprint 1 response persistence reused: existing
  `POST /api/v1/questionnaire-responses` remains the only scoring/persistence
  path.
- Sprint 2 D1 publish path reused: kiosk reads the active published version
  after admin publish.

## Result

- Report section: complete. Successful questionnaire submission creates a
  `report_sections` row.
- Public token / QR: complete. Submission returns `public_report_token`,
  `public_report_url`, and `qr_payload`.
- Admin response list: complete through
  `GET /api/v1/admin/questionnaire-responses`.
- PHQ-9 item 9 wording: complete. Item-9-positive sample returns
  `CONSULT_STAFF` and `requires_human_review: true`.
- Public report route: complete through `GET /api/v1/reports/{token}/public`.

## Scope Control

- Public output avoids raw answers, internal score payloads, internal bands,
  individual PHQ-9 item values, diagnostic labels, and treatment-advice wording.
- QR support is a QR-ready URL payload, not a QR management system.

## Evidence

- Low-risk live POST returned total `3`, `NORMAL_REFERENCE`, public token, and
  public report URL.
- Public report JSON returned only module title, public status code, safe
  summary, and disclaimer.
- Item-9-positive live POST returned total `1`, `CONSULT_STAFF`, and
  `requires_human_review: true`.
- Public report filter check passed for:
  `raw_answers|internal_score|internal_band|phq9_01|phq9_09|憂鬱症|中度憂鬱|重度憂鬱|診斷|治療建議`.
- Invalid SurveyJS JSON route returned:
  `INVALID_SURVEYJS_JSON`.

## Blocker Or Next Action

- Sprint 2 is closed. Sprint 3 can start from the published questionnaire,
  report/public-token path, and existing backend-owned scoring.
